node_modules/
dist/
*.log
.DS_Store
.env*
.logs/
coverage/
# exports del inspector (contienen headers/cookies del tráfico capturado)
*.har
# capturas de device sin redactar: scrubear antes de mover a fixtures/
fixtures/**/raw/

# capturas crudas de spikes: PII sin redactar (UDID, ECID, IMEI, teléfono)
.tmp/

# venv suelto de pruebas locales (el gestionado vive en ~/.sample-profiler/pmd3-venv)
.venv-ios/

# artefactos del e2e (traces y reporte HTML de Playwright)
test-results/
playwright-report/

# el repo usa bun.lock; npm se cuela al instalar browsers de Playwright
package-lock.json
