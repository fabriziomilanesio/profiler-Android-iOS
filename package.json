{
  "name": "@odaclick/evermore-android-profiler",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "bun": ">=1.3"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "fmt": "prettier --write src scripts docs README.md",
    "fmt:check": "prettier --check src scripts docs README.md",
    "build": "bun build --compile src/cli.ts --outfile dist/profiler"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node-forge": "^1.3.14",
    "prettier": "^3.6.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "node-forge": "^1.4.0"
  }
}
