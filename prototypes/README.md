# Prototipos (throwaway)

Prototipos descartables de los tickets 007 (dashboard realtime) y 020 (reporte HTML).
Son autocontenidos a propósito (vendor + assets duplicados) para abrirlos directo del
disco, offline. **No se mantienen**: la UI real vive en `src/ui/`. Se conservan porque
documentan las decisiones de diseño y el feedback humano que las motivó.
